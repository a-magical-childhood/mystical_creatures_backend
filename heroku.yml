build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - mkdir -p static
    - docker-compose exec web python manage.py collectstatic --noinput
run:
  web: gunicorn aMagicalChildhood.wsgi